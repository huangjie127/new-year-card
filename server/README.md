# qiji-horse-server

Short-link backend for saving/loading card state.

## Dev

```bash
npm install
npm run dev
```

## Endpoints

- `GET /api/health`
- `POST /api/share` body: scene state JSON
- `GET /api/share/:id`
